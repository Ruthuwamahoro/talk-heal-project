import DialogueSession from "@/components/Dashboard/DialogSession"

// Remove the named export and use default export directly
export default function DialogPage() {
    return (
        <DialogueSession />
    )
}